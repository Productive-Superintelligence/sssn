# Channel Processor

This example reads pending events from one subscription and writes baseline
analysis events to another channel.

It is intentionally small: the point is the SSSN service shape, not the
analysis strategy.
